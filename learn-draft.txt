1. 说说JavaScript中的变量提升吧？
JS代码执行时的机制，引擎会把var 变量&&函数声明提升到代码顶端。
这样就出现这种：未先定义，能先使用的现象。
为什么？
当初没有会计作用域，设计出把作用域内部的变量统一提升
提升执行性能。Js执行前会先编译（只执行一次，编译期间变量意思），避免了执行代码的时候重新解析变量函数
变量函数不会改变，编译一次就可（）预编译
增强容错
当初js脚本语言这么搞简单粗暴方便
问题：定义的变量会被系统给初值undefined造成迷惑、变量覆盖
声明 赋值
js 即时编译型语言 先编译后执行
编译阶段（变量提升放到内存中） =》 执行上下文（js代码执行的环境）、可执行代码（字节码）
执行上下文：「
变量提升的变量对象： 『』
作用域链
this
」
其他代码编程字节码
变量提升的缺陷
编译阶段 var 变量环境  let const 词法环境栈
执行上下文